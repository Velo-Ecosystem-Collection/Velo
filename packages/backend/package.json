{
  "name": "@repo/backend",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "benchmark:rate-limit-baseline": "node scripts/rate-limit-baseline.mjs",
    "dashboard": "convex dashboard",
    "dev": "convex dev",
    "lint:fix": "oxlint --fix convex && oxfmt --write convex && cd convex && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@carts1024/velo-wallets": "workspace:*",
    "@convex-dev/migrations": "^0.3.5",
    "@repo/observability": "workspace:*",
    "@repo/pdax": "workspace:*",
    "@repo/stellar": "workspace:*",
    "@upstash/redis": "^1.38.0",
    "convex": "^1.42.2"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "convex-test": "^0.0.53",
    "typescript": "5.9.2",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  }
}
